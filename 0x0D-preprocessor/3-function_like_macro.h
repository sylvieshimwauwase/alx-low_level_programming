#include <stdio.h>

#define ABS(x) ((x) < 0 ? -(x) : (x))

/** main - function that computes absolute value
 * Return:always 0 on success
 */
int main(void)
{
	printf("the absolute value of %d is %d", x, ABS(x));
	return (0);
}
